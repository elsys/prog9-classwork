Напишете функция, която обръща елементите на масив като използвате функцията `swap` от лекцията.

```
{10, 20, 30, 40, 50}

{50, 40, 30, 20, 10}
```

Ето я функцията от лекцията:

```
int swap(int *a, int *b) {
    int t = *a;
    *a = *b;
    *b = t;
    return 0;
}
```
